# grep for string in current directory (case sensitive and recursively)
sls <string> * -CaseSensitive -Recurse 2> $null

# grep line before and after matching line
Select-String <string> * -CaseSensitive -NotMatch 2> $null

