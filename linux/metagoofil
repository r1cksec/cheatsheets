# -d domain/ip, -t = type, -l max amount of search, -n max amount of downloads, -o = output dir
metagoofil -d <ipOrDomain> -t pdf -l 100 -n 25 -o <outputDir>

# possible filetypes pdf,doc,xls,ppt,odp,ods,docx,xlsx,pptx

